{
  "framework": "nextjs",
  "buildCommand": "next build",
  "outputDirectory": ".next",
  "installCommand": "npm cache clean --force && rm -f package-lock.json && npm install --legacy-peer-deps",
  "regions": ["iad1"],
  "env": {
    "NEXT_PUBLIC_APP_URL": "https://gigsenda.vercel.app"
  }
}
